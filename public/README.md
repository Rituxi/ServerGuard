# 静态资源目录 (public)

此文件夹是您的图床仓库。

### 如何上传图片：
1. 将您的图片（例如 `icon.png`）直接拖入此文件夹。
2. 在 VS Code 源代码管理中提交更改 (Commit)。
3. 点击同步更改 (Sync Changes / Push)。

Zeabur 会自动检测到更新并重新部署。
部署完成后，您的图片就会自动出现在网站首页，并提供永久有效的访问链接。